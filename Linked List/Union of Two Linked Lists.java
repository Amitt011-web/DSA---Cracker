class Solution {
    public Node makeUnion(Node head1, Node head2) {
        Node temp1 = head1, temp2 = head2;
        HashSet<Integer> set =new Hashet<>();
        while(temp1!=null){
            set.add(temp1.data);
            temp1 = temp1.next;
        }
        while(temp2!=null){
            if(!con)
        }
        
    }
}
